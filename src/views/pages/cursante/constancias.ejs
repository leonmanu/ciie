<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Constancia de Asistencia</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }
        .constancia {
            width: 100%;
            height: 100%;
            padding: 20px;
            box-sizing: border-box;
            page-break-inside: avoid; /* Evita que los registros se corten en saltos de página */
        }
        .w3-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .w3-bar img {
            width: 30px;
        }
        .solid {
            border: 1px solid #000;
            padding: 10px;
            margin-bottom: 20px;
            position: relative; /* Agregamos posición relativa */
        }
        .logo {
            position: absolute;
            top: 0;
            right: 0;
            width: 50px; /* Ajusta el ancho según tu necesidad */
        }
        .firma {
            margin-top: 50px;
            text-align: center;
        }
    </style>
</head>
<body>
    <% let i %>
    <% let encuentro %>

    <!-- Header -->
    <% i = 1 %>
    <!-- Contenido -->
    <div class="">
        <% cursantes.forEach(function(registro){ %>
            <% if (registro['Seleccione su/s curso/s'].includes(campo.clave+" - |") && (registro.encuentro1 ==  'TRUE' || registro.encuentro2 ==  'TRUE' || registro.encuentro3 ==  'TRUE')) { %>
                <div class="solid">
                    <img src="/img/logoCiie201.png" alt="Logo" class="logo"> <!-- Agregamos la imagen del logo -->
                    <p><strong>CERTIFICADO DE ASISTENCIA</strong></p>
                    <p>La Dirección del CIIE de Laferrere hace constar que el/la Docente <strong><%= registro['Apellido/s'] %>, <%= registro['Nombre/s'] %></strong>, DNI <strong><%= registro['Número de DNI'] %></strong> ha asistido al curso de capacitación en servicio en el área de <strong><%= campo.nombre %></strong> en nuestro establecimiento, en los siguientes días y horarios:</p>
                    <p>
                        <% for(var x=1; x<=cohorteUltima.nroEncuentros; x++) { %>
                            <% encuentro ='encuentro'+x %>
                            <% if(registro[encuentro]  == 'TRUE') { %>
                                <% let fechaSubstring = (encuentroFecha && [encuentro] !== undefined && encuentroFecha[encuentro] !== '') ? String(encuentroFecha[encuentro]).substring(8,10) + "/" + String(encuentroFecha[encuentro]).substring(5,7)  : '' %>
                                <strong><%= fechaSubstring %></strong> de <%= encuentroHora[encuentro] %>, 
                            <% } %>
                        <% } %>
                        de 2024
                    </p>
                    <div class="firma">
                        <p>_____________________________</p>
                        <p>Sello y Firma</p>
                    </div>
                </div>
                <% i++ %>
            <% } %>
        <% }) %>
    </div>
</body>
</html>
